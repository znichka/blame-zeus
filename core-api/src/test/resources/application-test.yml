app:
  llm:
    chat-api-key: test-key
    embedding-api-key: test-key
    chat-model: claude-haiku-4-5-20251001
    embedding-model: text-embedding-3-large

spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
