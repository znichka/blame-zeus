# Decision (per docs/TODO-stage2.md A2): requirements.txt-only, no pyproject.toml.
# This is a one-time offline script package (not published, no build backend needed).

openai>=1.0
psycopg2-binary
pgvector
tenacity>=8.2
python-dotenv
pytest>=8.0

# Stage 4 (ADR-004, ADR-008): offline seed-data extraction
instructor>=1.3.0
rapidfuzz>=3.0.0
anthropic

# Stage 4 Track B: 01_test_extraction.ipynb / 02_verify_conflicts.ipynb (ADR-004's own
# pipeline design calls for notebooks here, but nothing had declared the dependency yet)
jupyter
ipykernel
